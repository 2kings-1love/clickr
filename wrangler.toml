name = "getclickr-robots"
main = "index.js"
compatibility_date = "2025-11-12"

# For production, set workers_dev = false and add account_id and routes.
workers_dev = false

# Cloudflare account ID for 19segura83@gmail.com's Account
account_id = "89344a2f60b64a1465f5a6aa88a93f7d"

# Route to bind the Worker to the production domain
routes = ["getclickr.app/robots.txt"]

# Note: run 'wrangler publish' locally or via CI with an authenticated Cloudflare account that
# has permissions to manage Workers and routes for the getclickr.app zone.
